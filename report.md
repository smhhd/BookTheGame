# گزارش طراحی پایگاه داده سامانه رزرو و خرید بلیط مسابقات ورزشی

## ۱. مقدمه

این گزارش برای توضیح ساختار پایگاه داده پروژه «سامانه رزرو و خرید بلیط مسابقات ورزشی» تهیه شده است. هدف سیستم، مدیریت کاربران، مسابقات ورزشی، بلیط‌ها، سفارش‌ها، رزروها، پرداخت‌ها، گزارش مشکلات، کنسلی، استرداد وجه، قوانین کنسلی و برگزارکنندگان است.

طراحی پایگاه داده به‌صورت رابطه‌ای و برای PostgreSQL انجام شده است. در این طراحی، تلاش شده داده‌ها تا حد امکان نرمال‌سازی شوند، اطلاعات تکراری کاهش یابد و ارتباط بین موجودیت‌ها با کلیدهای خارجی و محدودیت‌های مناسب کنترل شود. همچنین برای افزایش درستی داده‌ها از `PRIMARY KEY`، `FOREIGN KEY`، `UNIQUE`، `CHECK` و برای بهبود کارایی از `INDEX` استفاده شده است.

---

## ۲. معرفی جداول و توجیه طراحی

### جدول `roles`

هدف این جدول نگهداری نقش‌های مجاز کاربران است. ستون‌های مهم آن `role_id` و `role_name` هستند. کلید اصلی جدول `role_id` است و روی `role_name` محدودیت `UNIQUE` وجود دارد.

در این جدول فقط دو نقش مجاز تعریف شده است: `spectator` و `support`. این محدودیت با `CHECK` کنترل می‌شود. جدا بودن این جدول باعث می‌شود نقش کاربران به‌صورت ساخت‌یافته و قابل کنترل ذخیره شود و مقدار نقش به‌صورت متن تکراری در جدول کاربران قرار نگیرد.

---

### جدول `provinces`

این جدول برای ذخیره استان‌ها استفاده می‌شود. ستون‌های اصلی آن `province_id` و `name` هستند. کلید اصلی `province_id` است و نام استان با `UNIQUE` کنترل می‌شود.

این جدول جدا شده تا اطلاعات جغرافیایی به‌صورت نرمال ذخیره شود و چند شهر بتوانند به یک استان متصل باشند.

---

### جدول `cities`

این جدول شهرها را ذخیره می‌کند. ستون‌های مهم آن `city_id`، `province_id` و `name` هستند. کلید اصلی `city_id` است و `province_id` کلید خارجی به جدول `provinces` است.

ترکیب `province_id` و `name` دارای `UNIQUE` است تا نام یک شهر در یک استان تکرار نشود. این جدول با کاربران، تیم‌ها، سالن‌ها/ورزشگاه‌ها و برگزارکنندگان ارتباط دارد. جدا بودن شهرها باعث می‌شود جستجو و گزارش‌گیری بر اساس شهر و استان بهتر انجام شود.

---

### جدول `users`

این جدول اطلاعات کاربران سامانه را نگهداری می‌کند. ستون‌های مهم آن شامل `user_id`، `role_id`، `city_id`، `first_name`، `last_name`، `email`، `phone`، `password_hash`، `status` و `registered_at` است.

کلید اصلی جدول `user_id` است. ستون `role_id` به جدول `roles` و ستون `city_id` به جدول `cities` متصل است. برای `email` و `phone` محدودیت `UNIQUE` تعریف شده و با `CHECK` کنترل شده که حداقل یکی از ایمیل یا شماره تلفن وجود داشته باشد. وضعیت کاربر نیز فقط می‌تواند یکی از `active`، `inactive` یا `blocked` باشد.

این جدول جدا طراحی شده چون کاربران موجودیت اصلی سامانه هستند و در سفارش، گزارش، پشتیبانی و کنسلی نقش دارند.

---

### جدول `support_users`

این جدول زیرمجموعه‌ای از کاربران پشتیبان را مشخص می‌کند. کلید اصلی آن `user_id` است که هم‌زمان کلید خارجی به جدول `users` محسوب می‌شود. ستون `support_role_id` نیز با مقدار ثابت ۲ کنترل می‌شود.

این جدول برای این طراحی شده که فقط کاربرانی که واقعاً نقش پشتیبان دارند بتوانند به‌عنوان بررسی‌کننده گزارش‌ها یا درخواست‌های کنسلی استفاده شوند. این کار باعث افزایش درستی داده‌ها می‌شود.

---

### جدول `organizers`

این جدول اطلاعات برگزارکنندگان مسابقات را ذخیره می‌کند. ستون‌های مهم آن `organizer_id`، `city_id`، `name`، `email`، `phone`، `status` و `created_at` هستند.

کلید اصلی `organizer_id` است. ستون `city_id` به جدول `cities` متصل است. برای `email` و `phone` محدودیت `UNIQUE` وجود دارد و با `CHECK` کنترل شده که حداقل یکی از آن‌ها مقدار داشته باشد. وضعیت برگزارکننده فقط می‌تواند `active` یا `inactive` باشد.

این جدول جدا طراحی شده چون قوانین کنسلی و مسابقات باید به برگزارکننده مشخصی وابسته باشند.

---

### جدول `sport_types`

این جدول نوع ورزش‌ها را نگهداری می‌کند، مانند فوتبال، والیبال و بسکتبال. ستون‌های آن `sport_type_id` و `name` هستند. کلید اصلی `sport_type_id` است و `name` یکتا است.

جدا بودن این جدول باعث می‌شود نوع ورزش در تیم‌ها، مسابقات، لیگ‌ها و قوانین کنسلی به‌صورت نرمال و بدون تکرار ذخیره شود.

---

### جدول `teams`

این جدول تیم‌های ورزشی را ذخیره می‌کند. ستون‌های مهم آن `team_id`، `sport_type_id`، `city_id` و `name` هستند.

کلید اصلی `team_id` است. `sport_type_id` به جدول `sport_types` و `city_id` به جدول `cities` متصل است. ترکیب `sport_type_id` و `name` یکتا است. همچنین ترکیب `team_id` و `sport_type_id` نیز یکتا شده تا در جدول مسابقات بتوان کنترل کرد که تیم‌ها با نوع ورزش مسابقه هماهنگ باشند.

این جدول جدا طراحی شده چون تیم‌ها در مسابقات مختلف تکرار می‌شوند و نباید نام تیم در هر مسابقه به‌صورت متنی تکرار شود.

---

### جدول `venues`

این جدول محل‌های برگزاری، مانند ورزشگاه، سالن یا arena را ذخیره می‌کند. ستون‌های مهم آن `venue_id`، `city_id`، `name`، `address` و `venue_type` هستند.

کلید اصلی `venue_id` است. `city_id` به جدول `cities` متصل است. نوع محل برگزاری با `CHECK` محدود شده و فقط می‌تواند `stadium`، `hall` یا `arena` باشد. ترکیب `city_id` و `name` نیز یکتا است.

این جدول جدا طراحی شده چون هر مسابقه در یک محل مشخص برگزار می‌شود و صندلی‌ها نیز به محل برگزاری وابسته‌اند.

---

### جدول `competitions`

این جدول لیگ‌ها یا تورنمنت‌ها را ذخیره می‌کند. ستون‌های مهم آن `competition_id`، `sport_type_id` و `name` هستند.

کلید اصلی `competition_id` است. `sport_type_id` به جدول `sport_types` متصل است. ترکیب `sport_type_id` و `name` یکتا است. همچنین ترکیب `competition_id` و `sport_type_id` برای کنترل هماهنگی لیگ با نوع ورزش مسابقه استفاده شده است.

این جدول جدا طراحی شده چون یک لیگ یا تورنمنت می‌تواند شامل چند مسابقه باشد.

---

### جدول `matches`

این جدول مسابقات ورزشی را ذخیره می‌کند. ستون‌های مهم آن `match_id`، `sport_type_id`، `competition_id`، `organizer_id`، `home_team_id`، `away_team_id`، `venue_id`، `match_datetime` و `status` هستند.

کلید اصلی `match_id` است. این جدول به `sport_types`، `competitions`، `organizers`، `teams` و `venues` متصل است. برای تیم میزبان و مهمان از کلید خارجی ترکیبی با `sport_type_id` استفاده شده تا تیم‌ها با نوع ورزش مسابقه هماهنگ باشند. همچنین با `CHECK` کنترل شده که تیم میزبان و مهمان یکسان نباشند.

این جدول جدا طراحی شده چون اطلاعات مسابقه، مانند تیم‌ها، زمان، مکان و برگزارکننده، برای تعداد زیادی بلیط مشترک است و نباید در جدول بلیط تکرار شود.

---

### جدول `ticket_categories`

این جدول رده‌های بلیط را نگهداری می‌کند، مانند عادی، ویژه یا VIP. ستون‌های آن `category_id`، `name` و `description` هستند.

کلید اصلی `category_id` است و `name` یکتا است. این جدول جدا طراحی شده چون هر بلیط به یک رده مشخص متصل است و اطلاعات رده نباید در همه بلیط‌ها تکرار شود.

---

### جدول `seats`

این جدول صندلی‌ها یا جایگاه‌های یک محل برگزاری را ذخیره می‌کند. ستون‌های مهم آن `seat_id`، `venue_id`، `section_name`، `row_number` و `seat_number` هستند.

کلید اصلی `seat_id` است. `venue_id` به جدول `venues` متصل است. ترکیب `venue_id`، `section_name`، `row_number` و `seat_number` یکتا است تا یک صندلی در یک محل برگزاری دوبار ثبت نشود. همچنین ترکیب `seat_id` و `venue_id` برای کنترل هماهنگی صندلی با محل برگزاری بلیط استفاده شده است.

این جدول جدا طراحی شده چون صندلی‌ها مستقل از مسابقه هستند و می‌توانند برای مسابقات مختلف در یک محل استفاده شوند.

---

### جدول `tickets`

این جدول بلیط‌های قابل فروش را ذخیره می‌کند. ستون‌های مهم آن `ticket_id`، `match_id`، `venue_id`، `category_id`، `seat_id`، `price`، `status` و `created_at` هستند.

کلید اصلی `ticket_id` است. این جدول به `matches`، `ticket_categories` و `seats` متصل است. ارتباط با `matches` و `seats` به‌صورت ترکیبی با `venue_id` کنترل شده تا صندلی انتخاب‌شده واقعاً متعلق به محل برگزاری همان مسابقه باشد. ترکیب `match_id` و `seat_id` یکتا است تا یک صندلی برای یک مسابقه دوبار فروخته نشود.

این جدول جدا طراحی شده چون هر بلیط یک آیتم قابل فروش مشخص برای یک مسابقه و یک صندلی است.

---

### جدول `orders`

این جدول سفارش‌های کاربران را ذخیره می‌کند. ستون‌های مهم آن `order_id`، `user_id`، `status`، `reserved_at` و `reserved_until` هستند.

کلید اصلی `order_id` است. `user_id` به جدول `users` متصل است. وضعیت سفارش با `CHECK` محدود شده و زمان پایان رزرو باید بعد از زمان شروع رزرو باشد.

این جدول برای پشتیبانی از خرید چند بلیط در یک سفارش طراحی شده است. به این ترتیب یک سفارش می‌تواند شامل چند رزرو باشد و پرداخت نیز روی کل سفارش انجام شود.

---

### جدول `reservations`

این جدول آیتم‌های رزرو داخل هر سفارش را ذخیره می‌کند. ستون‌های مهم آن `reservation_id`، `order_id`، `ticket_id`، `status`، `price_at_reservation`، `created_at` و `cancelled_at` هستند.

کلید اصلی `reservation_id` است. `order_id` به جدول `orders` و `ticket_id` به جدول `tickets` متصل است. مبلغ رزرو با `CHECK` غیرمنفی شده و وضعیت رزرو فقط مقدارهای مشخصی مثل `pending`، `paid`، `cancelled` و `expired` می‌گیرد.

این جدول جدا طراحی شده چون یک سفارش می‌تواند چند بلیط داشته باشد، اما هر رزرو به یک بلیط مشخص مربوط است.

---

### جدول `payments`

این جدول پرداخت‌های مربوط به سفارش‌ها را ذخیره می‌کند. ستون‌های مهم آن `payment_id`، `order_id`، `amount`، `method`، `status`، `paid_at` و `transaction_code` هستند.

کلید اصلی `payment_id` است. `order_id` به جدول `orders` متصل است. روش پرداخت با `CHECK` به مقدارهای `bank_card`، `wallet` و `crypto` محدود شده است. وضعیت پرداخت نیز با `CHECK` کنترل می‌شود. `transaction_code` یکتا است.

این جدول جدا طراحی شده چون پرداخت، موجودیتی مستقل از رزرو است و یک سفارش می‌تواند تلاش‌های پرداخت مختلف داشته باشد.

---

### جدول `cancellation_policies`

این جدول سیاست‌های کنسلی را بر اساس برگزارکننده و نوع ورزش ذخیره می‌کند. ستون‌های مهم آن `policy_id`، `organizer_id`، `sport_type_id`، `name`، `status` و `created_at` هستند.

کلید اصلی `policy_id` است. این جدول به `organizers` و `sport_types` متصل است. ترکیب `organizer_id`، `sport_type_id` و `name` یکتا است. همچنین ترکیب `policy_id`، `organizer_id` و `sport_type_id` برای کنترل هماهنگی قانون کنسلی با مسابقه استفاده شده است.

این جدول جدا طراحی شده چون قوانین کنسلی ممکن است برای برگزارکنندگان و نوع ورزش‌های مختلف متفاوت باشند.

---

### جدول `cancellation_policy_rules`

این جدول بازه‌های زمانی و درصد جریمه هر سیاست کنسلی را ذخیره می‌کند. ستون‌های مهم آن `rule_id`، `policy_id`، `min_hours_before_match`، `max_hours_before_match` و `penalty_percent` هستند.

کلید اصلی `rule_id` است. `policy_id` به جدول `cancellation_policies` متصل است. با `CHECK` کنترل شده که ساعات منفی نباشند، حد بالای بازه از حد پایین بیشتر باشد و درصد جریمه بین ۰ تا ۱۰۰ قرار گیرد. همچنین با `EXCLUDE USING gist` از هم‌پوشانی بازه‌های زمانی در یک سیاست جلوگیری شده است.

این جدول جدا طراحی شده چون هر سیاست کنسلی می‌تواند چند قانون زمانی داشته باشد.

---

### جدول `cancellation_requests`

این جدول درخواست‌های کنسلی یا تغییر جایگاه را ذخیره می‌کند. ستون‌های مهم آن `request_id`، `order_id`، `user_id`، `request_type`، `reason`، `status`، `requested_at`، `reviewed_by_support_id` و `reviewed_at` هستند.

کلید اصلی `request_id` است. این جدول با کلید خارجی ترکیبی به `orders(order_id, user_id)` متصل است تا فقط صاحب سفارش بتواند برای آن درخواست ثبت کند. همچنین `reviewed_by_support_id` به جدول `support_users` متصل است. نوع درخواست می‌تواند `single_ticket`، `whole_order` یا `change_seat` باشد.

این جدول جدا طراحی شده چون درخواست کنسلی یا تغییر جایگاه یک فرآیند مستقل از خود رزرو است و نیاز به بررسی پشتیبان دارد.

---

### جدول `cancellation_request_items`

این جدول آیتم‌های داخل هر درخواست کنسلی یا تغییر جایگاه را ذخیره می‌کند. ستون‌های مهم آن `request_id`، `order_id`، `request_type`، `reservation_id`، `ticket_id`، `match_id`، `item_action`، `requested_new_ticket_id`، `policy_id`، `policy_rule_id`، `organizer_id`، `sport_type_id`، `penalty_percent_applied`، `refund_amount` و `status` هستند.

کلید اصلی ترکیبی آن `request_id` و `reservation_id` است. این جدول به `cancellation_requests`، `reservations`، `tickets`، `matches`، `cancellation_policies` و `cancellation_policy_rules` متصل است.

در این جدول با کلیدهای خارجی ترکیبی کنترل شده که آیتم درخواست متعلق به همان سفارش باشد، بلیط جدید تغییر جایگاه مربوط به همان مسابقه باشد، و قانون کنسلی انتخاب‌شده مربوط به همان برگزارکننده و نوع ورزش مسابقه باشد.

این جدول جدا طراحی شده چون یک درخواست کنسلی کل سفارش می‌تواند شامل چند رزرو باشد و برای هر آیتم ممکن است جریمه و مبلغ استرداد جداگانه محاسبه شود.

---

### جدول `refunds`

این جدول اطلاعات استرداد وجه را ذخیره می‌کند. ستون‌های مهم آن `refund_id`، `payment_id`، `cancellation_request_id`، `order_id`، `amount`، `status` و `refunded_at` هستند.

کلید اصلی `refund_id` است. این جدول با کلید خارجی ترکیبی به `payments(payment_id, order_id)` و `cancellation_requests(request_id, order_id)` متصل است تا پرداخت و درخواست کنسلی مربوط به یک سفارش باشند.

این جدول جدا طراحی شده چون استرداد وجه یک فرآیند مالی جدا از پرداخت و درخواست کنسلی است و باید وضعیت مستقل داشته باشد.

---

### جدول `report_categories`

این جدول دسته‌بندی گزارش‌های کاربران را نگهداری می‌کند. ستون‌های آن `report_category_id` و `name` هستند. کلید اصلی `report_category_id` است و نام دسته‌بندی یکتا است.

این جدول جدا طراحی شده تا دسته‌بندی گزارش‌ها به‌صورت کنترل‌شده و بدون تکرار ذخیره شود.

---

### جدول `reports`

این جدول گزارش‌های ثبت‌شده توسط کاربران را ذخیره می‌کند. ستون‌های مهم آن `report_id`، `user_id`، `order_id`، `ticket_id`، `reservation_id`، `report_category_id`، `description`، `status`، `created_at`، `reviewed_by_support_id` و `reviewed_at` هستند.

کلید اصلی `report_id` است. این جدول به `users`، `tickets`، `orders`، `reservations`، `report_categories` و `support_users` متصل است. با `CHECK` کنترل شده که گزارش یا مستقیم روی یک بلیط باشد یا روی یک رزرو، نه هر دو هم‌زمان. همچنین در حالت گزارش روی رزرو، ارتباط با سفارش و کاربر کنترل می‌شود.

این جدول جدا طراحی شده چون گزارش مشکل یک موجودیت مستقل است و ممکن است توسط پشتیبان بررسی شود.

---

### جدول `facilities`

این جدول امکانات ویژه بلیط‌ها را ذخیره می‌کند، مانند پارکینگ یا پذیرایی. ستون‌های آن `facility_id` و `name` هستند. کلید اصلی `facility_id` است و `name` یکتا است.

این جدول جدا طراحی شده چون امکانات می‌توانند بین چند بلیط مشترک باشند و نباید به‌صورت متن تکراری در جدول بلیط ذخیره شوند.

---

### جدول `ticket_facilities`

این جدول واسط بین بلیط‌ها و امکانات است. ستون‌های آن `ticket_id` و `facility_id` هستند و کلید اصلی آن ترکیب همین دو ستون است.

`ticket_id` به جدول `tickets` و `facility_id` به جدول `facilities` متصل است. این جدول برای پیاده‌سازی رابطه چندبه‌چند بین بلیط و امکانات طراحی شده است؛ چون هر بلیط می‌تواند چند امکان داشته باشد و هر امکان نیز می‌تواند برای چند بلیط استفاده شود.

---

## ۳. روابط بین جداول

در طراحی نهایی، روابط اصلی به این شکل هستند:

* هر نقش می‌تواند به چند کاربر اختصاص داده شود.
* هر استان چند شهر دارد.
* هر شهر می‌تواند محل سکونت کاربران، محل تیم‌ها، محل ورزشگاه‌ها و محل برگزارکنندگان باشد.
* هر کاربر می‌تواند چند سفارش داشته باشد.
* هر سفارش می‌تواند چند رزرو داشته باشد.
* هر رزرو مربوط به یک بلیط است.
* هر پرداخت مربوط به یک سفارش است.
* هر مسابقه مربوط به یک نوع ورزش، یک برگزارکننده، یک محل برگزاری و دو تیم است.
* هر مسابقه می‌تواند چند بلیط داشته باشد.
* هر بلیط به یک مسابقه، یک صندلی و یک رده بلیط متصل است.
* هر درخواست کنسلی مربوط به یک سفارش و یک کاربر است.
* هر درخواست کنسلی یا تغییر جایگاه می‌تواند چند آیتم داشته باشد.
* هر آیتم کنسلی به یک رزرو و یک قانون کنسلی اختیاری متصل است.
* هر استرداد وجه به یک پرداخت و یک درخواست کنسلی متصل است.
* هر گزارش توسط یک کاربر ثبت می‌شود و یا به یک بلیط یا به یک رزرو مربوط است.
* امکانات بلیط‌ها از طریق جدول واسط `ticket_facilities` به بلیط‌ها متصل شده‌اند.

---

## ۴. محدودیت‌ها، کلیدها و ایندکس‌ها

### کلیدهای اصلی

تقریباً همه جدول‌ها دارای کلید اصلی مستقل هستند، مانند `user_id`، `ticket_id`، `order_id` و `reservation_id`. در جدول‌های واسط یا آیتمی، مانند `ticket_facilities` و `cancellation_request_items`، از کلید اصلی ترکیبی استفاده شده است.

کلیدهای اصلی باعث می‌شوند هر رکورد به‌صورت یکتا قابل شناسایی باشد.

---

### کلیدهای خارجی

در طراحی از کلیدهای خارجی برای حفظ ارتباط بین جداول استفاده شده است. برای مثال:

* `users.role_id` به `roles.role_id`
* `orders.user_id` به `users.user_id`
* `reservations.order_id` به `orders.order_id`
* `reservations.ticket_id` به `tickets.ticket_id`
* `payments.order_id` به `orders.order_id`
* `matches.organizer_id` به `organizers.organizer_id`
* `tickets.match_id, venue_id` به `matches.match_id, venue_id`
* `tickets.seat_id, venue_id` به `seats.seat_id, venue_id`

در چند بخش از کلید خارجی ترکیبی استفاده شده تا فقط وجود رکورد کنترل نشود، بلکه سازگاری منطقی هم بررسی شود؛ مثلاً صندلی باید متعلق به همان محل برگزاری مسابقه باشد.

---

### محدودیت‌های `UNIQUE`

محدودیت‌های یکتا برای جلوگیری از داده تکراری استفاده شده‌اند. نمونه‌ها:

* یکتا بودن ایمیل و شماره تلفن کاربران
* یکتا بودن نام استان
* یکتا بودن شهر در هر استان
* یکتا بودن نام تیم در هر نوع ورزش
* یکتا بودن صندلی در هر محل برگزاری
* یکتا بودن ترکیب `match_id` و `seat_id` در جدول `tickets`
* یکتا بودن `transaction_code` در جدول `payments`

این محدودیت‌ها مانع ثبت داده تکراری و ناسازگار می‌شوند.

---

### محدودیت‌های `CHECK` و مقدارهای enum-like

در این طراحی از enum مستقل PostgreSQL استفاده نشده است. به‌جای آن، مقدارهای مجاز با `CHECK` کنترل شده‌اند. برای مثال:

* وضعیت کاربر: `active`، `inactive`، `blocked`
* نوع محل برگزاری: `stadium`، `hall`، `arena`
* وضعیت مسابقه: `scheduled`، `postponed`، `cancelled`، `finished`
* وضعیت بلیط: `available`، `reserved`، `sold`، `cancelled`
* وضعیت سفارش: `pending`، `paid`، `partially_cancelled`، `cancelled`، `expired`
* روش پرداخت: `bank_card`، `wallet`، `crypto`
* نوع درخواست کنسلی: `single_ticket`، `whole_order`، `change_seat`

استفاده از `CHECK` باعث می‌شود مقدارهای نامعتبر وارد دیتابیس نشوند و کیفیت داده حفظ شود.

---

### جدول‌های واسط

دو جدول واسط مهم در طراحی وجود دارد:

1. `ticket_facilities`
   برای رابطه چندبه‌چند بین بلیط‌ها و امکانات.

2. `cancellation_request_items`
   برای اتصال یک درخواست کنسلی یا تغییر جایگاه به چند رزرو.

این جدول‌ها باعث می‌شوند روابط چندبه‌چند یا یک‌به‌چند پیچیده به‌صورت نرمال و قابل کنترل ذخیره شوند.

---

### ایندکس‌ها

در فایل ایندکس‌ها، چند ایندکس برای بهبود سرعت جستجو و گزارش‌گیری تعریف شده است. نمونه‌ها:

* `idx_users_city_id` برای جستجوی کاربران بر اساس شهر
* `idx_matches_datetime` برای مرتب‌سازی و جستجوی مسابقات بر اساس زمان
* `idx_tickets_match_id` برای پیدا کردن بلیط‌های یک مسابقه
* `idx_orders_user_id` برای پیدا کردن سفارش‌های یک کاربر
* `idx_reservations_order_id` برای پیدا کردن رزروهای یک سفارش
* `idx_payments_status_paid_at` برای گزارش‌گیری پرداخت‌ها بر اساس وضعیت و زمان
* `idx_reports_user_id`، `idx_reports_ticket_id` و `idx_reports_reservation_id` برای بررسی گزارش‌ها

همچنین چند ایندکس یکتا و شرطی وجود دارد:

* `uq_active_reservation_per_ticket`
  جلوگیری از رزرو فعال یا پرداخت‌شده تکراری برای یک بلیط.

* `uq_success_payment_per_order`
  جلوگیری از بیش از یک پرداخت موفق برای یک سفارش.

* `uq_success_refund_per_cancellation_request`
  جلوگیری از بیش از یک استرداد موفق برای یک درخواست کنسلی.

* `uq_active_policy_per_organizer_sport`
  جلوگیری از وجود بیش از یک سیاست کنسلی فعال برای یک برگزارکننده و یک نوع ورزش.

* `uq_active_cancellation_item_per_reservation`
  جلوگیری از ثبت چند آیتم کنسلی فعال برای یک رزرو.

این ایندکس‌ها علاوه بر کارایی، در بعضی موارد نقش کنترلی و تضمین‌کننده درستی داده نیز دارند.

---

## ۵. جمع‌بندی

طراحی پایگاه داده این پروژه بر اساس تفکیک موجودیت‌های اصلی انجام شده است. اطلاعات کاربران، نقش‌ها، مسابقات، تیم‌ها، ورزشگاه‌ها، بلیط‌ها، سفارش‌ها، رزروها، پرداخت‌ها، کنسلی‌ها، استردادها و گزارش‌ها در جدول‌های جداگانه ذخیره شده‌اند.

این تفکیک باعث کاهش افزونگی داده، افزایش خوانایی طراحی و ساده‌تر شدن گزارش‌گیری می‌شود. استفاده از کلیدهای خارجی، محدودیت‌های یکتا، محدودیت‌های بررسی مقدار و ایندکس‌ها نیز باعث شده طراحی از نظر درستی داده و کارایی قابل دفاع باشد.

از نظر نیازهای پروژه، طراحی نهایی امکان خرید چند بلیط در یک سفارش، پرداخت گروهی، کنسلی تکی، کنسلی کل سفارش، تغییر جایگاه، استرداد جزئی، مدیریت برگزارکننده، قوانین کنسلی و گزارش مشکلات را پوشش می‌دهد.

---

## ۶. نکات قابل بررسی، در صورت وجود

چند نکته در طراحی وجود دارد که در سطح پایگاه داده به‌صورت کامل enforce نشده و معمولاً در لایه بک‌اند، Stored Procedure یا Trigger کنترل می‌شود:

1. هماهنگ‌ماندن وضعیت‌ها
   وضعیت‌های `tickets`، `orders`، `reservations` و `payments` باید در عملیات واقعی سیستم هماهنگ به‌روزرسانی شوند.

2. کامل بودن کنسلی کل سفارش
   در حالت `whole_order` باید بررسی شود که همه رزروهای سفارش داخل `cancellation_request_items` ثبت شده باشند.

3. کنترل سقف مجموع استردادها
   مجموع مبلغ استردادها نباید از مبلغ پرداخت‌شده بیشتر شود.

4. تطبیق دقیق قانون کنسلی با زمان مسابقه
   ساختار جدول‌ها قانون کنسلی را به برگزارکننده و نوع ورزش متصل می‌کند، اما انتخاب قانون مناسب بر اساس فاصله زمانی تا مسابقه باید در منطق اجرایی سیستم انجام شود.

5. Redis و OTP
   در ساختار SQL، جدول جداگانه‌ای برای OTP وجود ندارد؛ زیرا در طراحی پروژه، OTP و کش داده‌ها برای Redis در نظر گرفته شده‌اند، نه پایگاه داده رابطه‌ای.

این موارد نقص ساختاری مستقیم محسوب نمی‌شوند، اما برای پیاده‌سازی کامل سیستم باید در فازهای بعدی پروژه مورد توجه قرار گیرند.
